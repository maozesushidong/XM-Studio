# 项目目标

开发“先马·AI Studio”企业桌面应用，提供模型对话、多模态、文件操作、网页预览、文档生成和技能能力；使用钉钉登录，并按钉钉用户在本机隔离数据。当前新增目标是 Windows 自动更新且覆盖安装不丢历史对话。

# 已完成内容

- Electron + 原生 HTML/CSS/JavaScript 桌面端。
- 基础修复包为 `1.0.1` / 内部版本 `1.4.54`，已生成自动更新测试包 `1.0.2` / 内部版本 `1.4.56`；故障版 `1.4.55` 已撤回。
- 默认对话模型 `gpt-5.6-luna`，图片模型 `gpt-image-2`。
- 流式对话、多会话并行、停止生成、模型切换。
- 任意会话支持文件创建、修改、运行、预览、复制和下载。
- 支持 DOCX、PPTX、XLSX、PDF、开放文档和常见文本格式。
- 钉钉登录、会话续期、退出和用户本地数据隔离。
- 本地/在线技能安装、后台安装、技能结果与对话绑定隔离。
- 稳定用户数据路由，兼容多个历史 `%APPDATA%` 目录。
- 更新前备份全部用户的对话 LocalStorage；记录缺失时自动恢复。
- 客户端启动检查、5 分钟轮询、SSE 实时更新通知。
- 设置页显示当前版本、更新状态、上次检查时间，支持手动检查和关闭/开启自动检查。
- 设置页可查看新版本号、发布时间、安装包大小、更新标题、更新说明和最近 12 条更新记录。
- 安装包后台下载、SHA-256 校验、Ed25519 清单验签和静默覆盖安装。
- 更新失败重试可复用已下载且校验通过的安装包，损坏缓存会自动删除并重新下载。
- 更新发布后台：分片上传、失败重试、断点续传、填写说明、强制更新、发布、撤回和永久删除文件。
- 服务器永久删除仅允许 `revoked` 版本，安装文件会删除，审计记录保留并标记为 `deleted`。
- 修复更新前备份对 Electron `flushStorageData()` 空返回值处理错误；已生成 `1.0.1` 手动修复包和修复后的 `1.0.2` 升级包。
- 更新服务已部署到服务器 `/opt/xianma-update-service`。
- 本地语法、更新客户端、更新服务、Electron 和打包应用回归通过。

# 当前进行中

- 已生成并交付带设置页更新入口的 `1.0.1` 过渡安装包及 `1.0.2` 测试升级包。
- 待先手动安装 `1.0.1 自动更新修复版`，再将修复后的 `1.0.2` 上传后台完成真实升级验收。
- 准备更新专用域名、HTTPS 和 Windows 代码签名证书。

# 当前 Bug / 阻塞

- 安装包 Authenticode 状态为 `NotSigned`，Windows 可能显示未知发布者。
- 当前更新地址使用公网 IP 的 HTTP，只适合内测；管理员发布操作必须尽快迁移到 HTTPS。
- 大文件上传失败问题已通过分片重试解决；首次使用新版后台需要刷新页面。
- 原始 `1.0.1` 更新器无法自动启动任何新安装包，必须先手动覆盖修复版；当前故障发布已撤回。
- 已安装旧版不含更新客户端，必须手动安装一次 `1.0.1` 过渡包；当前已安装 `1.0.1` 的电脑可直接测试升级到 `1.0.2`。
- 两个旧数据目录中分别存在不同会话时，目前选择最近使用的完整资料目录，不合并两个 LevelDB。

# 关键设计决策

- 显示版本与内部更新版本分离，避免历史 `1.4.53` 阻止 `1.0.1` 更新。
- 测试升级包：显示版本 `1.0.2`，内部版本 `1.4.56`；测试通过后继续递增内部版本。
- 更新清单使用 Ed25519 签名，安装包使用 SHA-256 校验。
- 更新前保存逻辑对话备份，不逐文件合并 Chromium LevelDB。
- 安装只替换 `Program Files`，不删除 `%APPDATA%` 和用户文件。
- 自动更新仍需要 Windows UAC 确认；不创建高权限常驻更新服务。
- 模型 Key、钉钉 Secret和更新管理密码不得进入前端或公开文档。

# 重要文件

- `electron/main.js`：主进程、用户数据路由和更新客户端接入。
- `electron/updater.js`：检查、SSE、下载、验签、备份和启动安装。
- `electron/update-config.json`：显示版本、内部版本和更新地址。
- `electron/update-signing-public.pem`：更新清单验签公钥。
- `electron/preload.js`：更新与恢复 IPC。
- `renderer/app.js`：对话恢复和更新状态提示。
- `build/先马智能体安装器.iss`：Inno Setup 覆盖安装和自动重启。
- `update-service/server.js`：更新发布 API、SSE、签名和下载。
- `update-service/public/admin.html`：管理员发布页面。
- `update-service/docker-compose.yml`：服务器容器部署。
- `scripts/build-offline-release.js`：离线一键打包。
- `文档/自动更新使用说明.md`：小白发布操作说明。
- `交付/自动更新后台信息.txt`：本机管理员地址和密码，禁止外发。
- `交付/先马·AI Studio Setup 1.0.1 自动更新修复版.exe`：一次性手动修复包，SHA-256 为 `88AB3EC97EF5BCAD4F41DC784690C2B3047E4AA3B2EEC08B101A57BCC59ABDE9`。
- `交付/先马·AI Studio Setup 1.0.2.exe`：修复后的自动更新测试包，内部版本 `1.4.56`，SHA-256 为 `7F81B0C0FCF7DE84B6AC5A86024C583CE2B34CBBACAB60C20EF0C90A42470583`。

# 下一步计划

1. 手动安装 `1.0.1 自动更新修复版`，再发布并自动升级到修复后的 `1.0.2`，确认会话和钉钉身份保留。
2. 购买 Windows 代码签名证书并重新签名。
3. 准备更新专用域名，配置 HTTPS 后再全员发布。
4. 备份服务器 `data` 和 `secrets` 目录。

# 注意事项

- 不要删除 `%APPDATA%` 下任何先马历史目录。
- 不要把内部更新版本改回 `1.4.54`，也不要复用已经发布过的内部版本。
- 不要逐文件复制或合并 LocalStorage 的 LevelDB。
- 不要把更新签名私钥复制进桌面安装包。
- 不要公开 `交付/自动更新后台信息.txt`。
- 本次会话暴露过服务器 root 密码，部署结束后必须更换。

# 推荐启动 Prompt

继续开发“先马·AI Studio”。工程目录为 `C:\Users\Administrator\Documents\Codex\2026-07-09\w\work\centaur-desktop`。先阅读 `先马智能体.md` 和 `文档\自动更新使用说明.md`。基础修复包为 `1.0.1 / 1.4.54`，测试升级包为 `1.0.2 / 1.4.56`，故障版 `1.4.55` 已撤回，更新服务已部署。优先完成真实自动更新和旧对话保留验收，再配置域名、HTTPS 和 Windows 代码签名。任何新版本的内部版本必须高于 `1.4.56`，不得删除用户数据，不得输出模型 Key、钉钉 Secret、更新管理密码或更新签名私钥。
