# 先马·AI Studio 完整版本说明

更新时间：2026-08-13  
适用平台：Windows x64  
整理范围：项目原型阶段至 `1.3.0.3` 本地修正版

## 一、说明与可信度

本文根据现有原型、历史安装包、构建目录、打包配置、项目记忆、发布说明、更新记录、Git 记录和 `app.asar` 源码差异整理。

可信度标记：

- **已确认**：有发布文档、更新记录或源码差异支持。
- **部分确认**：能确认安装包和代码变化，但没有完整发布说明。
- **仅有归档**：只能确认该安装包存在，无法准确还原功能变化。
- **未发布**：已开发或已构建，但未作为正式更新下发。

> 早期没有持续维护正式 Changelog。本文不会为缺少记录的版本编造功能。

## 二、版本号规则和历史混用

项目历史上出现过三套版本概念：

1. **早期内部构建号**：`1.4.4` 至 `1.4.56`，主要用于频繁打包和测试。
2. **对外显示版本**：`1.0.0`、`1.0.1`、`1.0.2`、`1.0.3`、`1.3.0.2`、`1.3.0.3`。
3. **未来规划版本**：计划中的对外 `1.4.0`，代表公司公共技能平台，不等于早期内部 `1.4.x` 构建号。

已核实的同包映射：

| 对外版本 | 早期内部版本 | 说明 |
| --- | --- | --- |
| `1.0.0` | `1.4.48` | 两个安装包 SHA-256 完全相同，是同一构建的两个文件名。 |
| `1.0.1` | `1.4.52` | 两个安装包 SHA-256 完全相同，是同一构建的两个文件名。 |
| `1.0.1` | `1.4.54` | 后续又以相同对外版本制作了自动更新过渡包，内容已不同。 |
| `1.0.2` | `1.4.55` | 更新器故障测试包，未正式发布。 |
| `1.0.2` | `1.4.56` | 修复后的自动更新测试包。 |
| `1.0.2` 起 | 与显示版本统一 | 新通道 `stable-v2` 改为显示版本与内部版本一致。 |

产品名称演进：

```text
先马智能体
→ 先马·Centaur
→ 先马·AI Studio
```

## 三、版本总览

| 阶段 | 版本 | 状态 | 核心变化 |
| --- | --- | --- | --- |
| 原型期 | 无正式版本 | 原型/方案 | 登录、主对话、搜索、已安排、技能和结果页设计。 |
| 高频开发期 | `1.4.4`–`1.4.53` | 内部构建 | 从桌面壳逐步完善为对话、附件、技能、文件、网页预览和企业品牌客户端。 |
| 首次对外版 | `1.0.0` | 历史归档 | 对应内部 `1.4.48`。 |
| 自动更新过渡 | `1.0.1` | 历史发布 | 对应内部 `1.4.52`，后续追加 `1.4.54` 更新器修复包。 |
| 统一重装版 | `1.0.2` | 历史发布 | 统一版本号、切换 `stable-v2`、本机操作、数据路由和自动更新。 |
| 企业权限版 | `1.0.3` | 历史发布 | 通用电脑应用操作、会话迁移、使用统计、MAPMS 权限接入。 |
| 公司技能平台 | `1.0.4` | 本地构建，未发布 | 公司技能创建、提交、审核、安装、更新和匿名统计。 |
| 会议模型版 | `1.3.0.2` | 已交付/历史生产版 | 固定内置“东哥会议模型”，明确排除 `1.4.0` 公司技能平台。 |
| 多媒体与路由版 | `1.3.0.3` | 已上传更新服务器 | 双服务路由、音视频处理、本机应用和文件能力增强。 |
| 当前修正版 | `1.3.0.3` 本地修正版 | 未上传 | 回答复制、图片意图修复、更新断点续传和下载体验优化。 |

## 四、原型与基础开发阶段

### 原型阶段（2026-07-09 起）

**改进内容**

- 确立钉钉登录、主对话、搜索、已安排、技能、技能表单和结果页。
- 确立产品方向：把企业 AI 能力包装为可安装的 Windows 桌面应用。
- 确立 Codex 风格的左侧会话列表、中央对话区和底部输入区。

**实现方式**

- 先使用独立 HTML 原型确认页面结构和品牌视觉。
- 后续迁移到 Electron + 原生 HTML/CSS/JavaScript 桌面架构。
- 系统能力放在 Electron 主进程，通过 Preload IPC 提供给前端。

## 五、早期内部构建版本

### 1.4.4–1.4.21：早期安装器阶段

| 版本 | 日期 | 状态 | 已知改进及实现情况 |
| --- | --- | --- | --- |
| `1.4.4` | 2026-07-16 | 仅有归档 | 当前能找到的最早安装包，产品名为“先马智能体”；无可比对的上一版源码。 |
| `1.4.5` | 2026-07-17 | 仅有归档 | 早期小步修订包；现有资料无法准确确认具体功能差异。 |
| `1.4.6` | 无归档 | 无记录 | 未找到安装包、源码或发布记录。 |
| `1.4.7` | 无归档 | 无记录 | 未找到安装包、源码或发布记录。 |
| `1.4.8` | 无归档 | 无记录 | 未找到安装包、源码或发布记录。 |
| `1.4.9` | 无归档 | 无记录 | 未找到安装包、源码或发布记录。 |
| `1.4.10` | 无归档 | 无记录 | 未找到安装包、源码或发布记录。 |
| `1.4.11` | 无归档 | 无记录 | 未找到安装包、源码或发布记录。 |
| `1.4.12` | 2026-07-17 | 仅有归档 | 安装包存在，具体改进记录缺失。 |
| `1.4.13` | 2026-07-17 | 仅有归档 | 安装包存在，具体改进记录缺失。 |
| `1.4.14` | 2026-07-17 | 仅有归档 | 安装包存在，具体改进记录缺失。 |
| `1.4.15` | 2026-07-17 | 仅有归档 | 安装包存在，具体改进记录缺失。 |
| `1.4.16` | 2026-07-17 | 仅有归档 | 安装包存在，具体改进记录缺失。 |
| `1.4.17` | 2026-07-21 | 仅有归档 | 安装包和 ZIP 存在，具体改进记录缺失。 |
| `1.4.18` | 2026-07-21 | 仅有归档 | 安装包和 RAR 存在，具体改进记录缺失。 |
| `1.4.19` | 无归档 | 无记录 | 未找到安装包、源码或发布记录。 |
| `1.4.20` | 2026-07-21 | 部分确认 | 安装包从约 450 MB 增至约 496 MB，说明内置运行资源有明显调整；无法确认具体模块。 |
| `1.4.21` | 2026-07-21 | 仅有归档 | `1.4.20` 后的小步构建，具体改进记录缺失。 |

### 1.4.22（2026-07-21）

**改进内容**

- 形成目前能提取并比对的第一份完整桌面应用基线。
- 已包含 Electron 主进程、Preload、安全前端、内置运行时和 Windows 安装器。

**实现方式**

- 安装包携带 `app.asar`、Node.js、能力运行时和运行工具。
- 产品名仍为“先马智能体”，AppId 为 `com.xianma.centaur.desktop`。

### 1.4.23（2026-07-22）

**改进内容**

- 新增本地和在线技能安装基础能力。
- 增加技能结构和 `SKILL.md` 元数据校验。

**实现方式**

- 主进程新增用户私有技能目录和文件监视。
- 支持在线技能库来源。
- 解析 YAML Front Matter，校验技能 `name` 和 `description`。
- 增加压缩包路径、文件数量、解压体积和单文件安全检查。

### 1.4.24（2026-07-22）

**改进内容**

- 扩展技能安装格式。
- 提高 Windows 下技能覆盖安装的稳定性。

**实现方式**

- 支持 `.md`、`.markdown`、`.zip` 和包含 `SKILL.md` 的文件夹。
- 文件选择器增加技能文件和技能文件夹入口。
- 使用暂存目录、备份目录和重命名重试实现近似原子安装，处理 `EPERM`、`EBUSY`、`EACCES`。

### 1.4.25（2026-07-22）

**改进内容**

- 扩展图片、录音、音频和视频附件支持。
- 技能表单和对话可携带多媒体文件。

**实现方式**

- 增加常见图片、音频、视频扩展名与 MIME 映射。
- 附件统一经过主进程校验和分类。
- 大文件保留本地路径，适合交给本地能力运行时处理。

### 1.4.26（2026-07-22）

**改进内容**

- 调整企业模型服务和前端模型选项。
- 放宽技能和多媒体文件处理能力。

**实现方式**

- 模型配置改为企业 API 兼容接口。
- 大文件不再全部塞入 WebSocket，请求仅传必要内容或本地绝对路径。
- 运行时图片、音频和视频能力统一打开，并提高媒体处理上限。

### 1.4.27（无归档）

- 未找到安装包、源码或发布记录，无法确认是否真实形成过可交付构建。

### 1.4.28–1.4.35（2026-07-22）

| 版本 | 状态 | 改进说明 |
| --- | --- | --- |
| `1.4.28` | 部分确认 | 与 `1.4.26` 的核心应用代码一致，主要是打包或版本号迭代。 |
| `1.4.29` | 部分确认 | 与 `1.4.26` 的核心应用代码一致，主要是打包或版本号迭代。 |
| `1.4.30` | 部分确认 | 与 `1.4.26` 的核心应用代码一致，主要是打包或版本号迭代。 |
| `1.4.31` | 部分确认 | 与 `1.4.26` 的核心应用代码一致，主要是打包或版本号迭代。 |
| `1.4.32` | 部分确认 | 与 `1.4.26` 的核心应用代码一致，主要是打包或版本号迭代。 |
| `1.4.33` | 部分确认 | 与 `1.4.26` 的核心业务代码一致，仅包内版本元数据变化。 |
| `1.4.34` | 部分确认 | `app.asar` 仍写为 `1.4.33`，属于重组装包，未检测到业务代码变化。 |
| `1.4.35` | 部分确认 | `app.asar` 仍写为 `1.4.33`，属于重组装包，未检测到业务代码变化。 |

### 1.4.36（2026-07-22）

**改进内容**

- 修正默认模型和模型选择项。

**实现方式**

- 默认文字模型统一为 `gpt-5.6-luna`。
- 修正 Luna、Terra 等模型值映射，避免前端选项与后端实际模型不一致。

### 1.4.37（2026-07-23）

**改进内容**

- 技能安装支持后台静默运行。
- 用户可在安装过程中切换页面。

**实现方式**

- 主进程建立技能安装任务队列和任务状态表。
- 通过 IPC 广播排队、安装中、成功和失败状态。
- 前端接收完成通知，不再要求用户一直停留在技能页。

### 1.4.38（2026-07-23）

**改进内容**

- 减少对第三方技能格式的过度限制。
- 提高不标准 Markdown 技能的识别率。

**实现方式**

- YAML 解析支持 `>` 和 `|` 多行写法。
- 非标准名称自动规范化，缺失名称可生成稳定标识。
- 可从正文推导技能描述。
- 技能文件数量上限从 500 提高到 5000，并增加目录深度安全检查。

### 1.4.39（2026-07-23）

**改进内容**

- 优化在线技能加载和安装成功率。
- 在线技能列表提前过滤明显不可安装的结果。

**实现方式**

- 新增在线技能预检缓存。
- 安装前检查远端 `SKILL.md`、名称和描述。
- 对不符合要求的在线技能返回明确原因，减少长时间等待后才失败。

### 1.4.40（2026-07-23）

**改进内容**

- 产品改名为“先马·Centaur”。
- 技能执行结果直接展示正文，不再只显示“已完成”。
- HTML 结果可直接打开应用内网页预览。
- 修复技能页面操作污染全部会话的问题。

**实现方式**

- 主进程从模型流式响应或生成的文本文件提取真实正文。
- 技能结果不再强制制造空 Markdown 文件。
- 会话状态升级技能绑定版本，清除旧的全局错误绑定。
- Electron `BrowserWindow` 提供隔离的网页预览窗口。

### 1.4.41（2026-07-23）

**改进内容**

- 普通知识问答不再被已选技能错误接管。
- 改善天气、首都、日期等简单问题的响应。
- 优化图片模型误路由。

**实现方式**

- 前端增加一般知识问题识别。
- 没有附件的简单事实问题临时绕过技能提示词。
- 只有明确图片生成意图才进入图片接口。

### 1.4.42（2026-07-23）

**改进内容**

- 修复技能执行只返回初始化问候、没有处理实际任务的问题。
- 进一步修复技能标签跨会话污染。

**实现方式**

- 清理运行时默认 `BOOTSTRAP.md`，写入企业助手身份和隐私约定。
- 技能请求强制携带用户本次输入和附件，并禁止初始化问候。
- 技能绑定状态升级为按会话版本，技能页“使用”不再写入所有对话。

### 1.4.43–1.4.46（2026-07-23）

| 版本 | 状态 | 改进说明 |
| --- | --- | --- |
| `1.4.43` | 部分确认 | 核心应用代码与 `1.4.42` 一致，属于版本/安装器迭代。 |
| `1.4.44` | 部分确认 | 核心应用代码与 `1.4.42` 一致，属于版本/安装器迭代。 |
| `1.4.45` | 部分确认 | 核心应用代码与 `1.4.42` 一致，属于版本/安装器迭代。 |
| `1.4.46` | 部分确认 | 核心应用代码与 `1.4.42` 一致，安装包存在不同组装体积。 |

### 1.4.47（2026-07-23）

**改进内容**

- 技能结果可生成并展示 DOCX 文件。
- 文件卡显示文件名和绝对路径。

**实现方式**

- 技能结果默认构造 DOCX 文档任务并持久化生成文件。
- 结果页和文件卡增加路径展示与下载入口。

### 1.4.48（2026-07-23）

**改进内容**

- 技能页面增加多轮对话提示。
- 作为首个对外 `1.0.0` 的实际构建。

**实现方式**

- 在技能列表加入“技能最好在对话中使用，部分在线 skill 需要多轮对话”的提示。
- 同一安装包同时以 `1.4.48` 和 `1.0.0` 文件名归档。

### 1.4.49（2026-07-24）

**改进内容**

- 去除产品层面的时间、次数和附件大小限制。
- 长时间技能和在线技能不再因客户端固定超时中断。

**实现方式**

- 将运行时超时设置为接近进程生命周期的有效大值。
- 文件继续按本地路径处理，保留必要的路径和压缩包安全检查。
- 模型、图片和在线技能请求保留用户主动停止能力。

### 1.4.50（2026-07-24）

**改进内容**

- 支持多会话并行运行能力任务。
- 改善能力调用过程的可见状态。

**实现方式**

- 网关客户端从全局单例改为按“用户 + 会话”缓存。
- 不同会话使用独立连接和任务状态。
- 文件读取、写入、文档生成和命令运行显示中文操作名称。

### 1.4.51（2026-07-24）

- 仅检测到极小的主进程调整和版本更新，没有可确认的新用户功能。

### 1.4.52（2026-07-24）

**改进内容**

- 修复技能运行结束后只有状态、没有最终正文的问题。
- 改善在线网页技能识别。
- 作为对外 `1.0.1` 的首个实际构建。

**实现方式**

- 按 `runId` 接收、缓存并合并流式事件。
- 任务结束时提取最终消息正文，而不是只返回“任务已完成”。
- 增加网页技能 URL 和 HTML 实体解析。
- 同一安装包同时以 `1.4.52` 和 `1.0.1` 文件名归档。

### 1.4.53（2026-07-29）

**改进内容**

- 产品正式改名为“先马·AI Studio”。
- 开始统一新旧配置和数据目录。

**实现方式**

- AppId 改为 `com.xianma.ai-studio.desktop`。
- 公共配置迁移到 `C:\ProgramData\XianmaAIStudio`。
- 用户数据默认迁移到 `%APPDATA%\xianma-ai-studio`。
- 启动时合并旧配置并复制缺失的历史目录内容。

### 1.4.54 / 对外 1.0.1 自动更新修复版（2026-07-30）

**改进内容**

- 首次形成可用的自动更新过渡客户端。
- 设置页增加检查版本和更新说明。

**实现方式**

- 增加启动检查、5 分钟轮询和 SSE 更新通知。
- 增加后台下载、SHA-256 校验、Ed25519 清单验签和覆盖安装。
- 更新前备份会话 LocalStorage。
- 修复 Electron `flushStorageData()` 空返回值导致无法启动安装包的问题。

**状态**

- 旧 `1.0.1` 更新器本身存在故障，用户必须先手动覆盖安装此修复版。
- 修复包 SHA-256：`88AB3EC97EF5BCAD4F41DC784690C2B3047E4AA3B2EEC08B101A57BCC59ABDE9`。

### 1.4.55 / 对外 1.0.2 故障测试包（2026-07-30）

- 用于测试从 `1.0.1` 自动升级。
- 更新器启动安装流程存在问题，版本已撤回，未作为正式版本继续分发。

### 1.4.56 / 对外 1.0.2 自动更新测试包（2026-07-30）

**改进内容**

- 修复 `1.4.55` 的安装启动问题。
- 完成一次真实的自动下载、安装和重启记录。

**实现方式**

- 更新失败时复用已下载且校验通过的安装包。
- 损坏缓存自动删除后重新下载。
- 更新历史记录确认已完成 `available → downloaded → installing → installed`。

## 六、对外版本说明

## 1.0.0：首个对外归档版

```text
实际内部构建：1.4.48
产品名：先马·Centaur
状态：历史归档
```

**主要改进**

- 形成可安装的 Windows 桌面客户端。
- 已具备多会话对话、模型调用、文件附件、技能安装、技能结果和网页预览等基础能力。
- 技能执行结果开始直接展示，技能绑定开始按会话隔离。

**怎么实现**

- Electron 封装桌面界面。
- 主进程连接企业模型与本地能力运行时。
- Preload IPC 隔离前端和系统权限。
- 用户数据保存在本机 Electron 用户目录。

## 1.0.1：技能执行与自动更新过渡版

```text
首个内部构建：1.4.52
自动更新修复构建：1.4.54
状态：历史发布
```

**主要改进**

- 技能执行支持流式结果和最终正文。
- 在线网页技能识别更完整。
- 产品名称逐步从“先马·Centaur”切换到“先马·AI Studio”。
- 加入客户端更新检查、下载、校验、安装和重启。

**怎么实现**

- 按技能运行 ID 聚合事件流，解决结果空白。
- 新增独立更新服务、发布后台和签名清单。
- 更新前创建逻辑会话备份，安装器只覆盖程序目录。

**版本注意**

- 对外同为 `1.0.1` 的安装包曾有多种内部构建，不能只按文件名判断新旧。
- 应以 SHA-256、内部版本和构建时间判断。

## 1.0.2：统一版本重装版

```text
显示版本：1.0.2
内部版本：1.0.2
更新通道：stable-v2
状态：历史发布/迁移基线
```

**主要改进**

- 显示版本和内部版本统一。
- 新更新通道切换为 `stable-v2`，避开旧 `stable / 1.4.56` 版本序列。
- 所有会话默认具备文件、文档、命令和浏览器能力。
- 支持 Word、PPT、Excel、PDF、图片、代码和 HTML 网页。
- 文件卡支持绝对路径、预览、复制和下载。
- 支持多会话并行、停止生成和前端真实模型切换。
- 支持钉钉登录和本机多用户隔离。
- 增强卸载重装后的历史数据保留。

**怎么实现**

- 本机操作统一在 Electron 主进程执行，前端只走 IPC。
- 新增文件读写、复制、移动、删除、搜索、打开和命令工具。
- 危险操作通过确认事件和本地审计处理。
- `user-data-route.json` 记录稳定用户数据目录。
- 启动时扫描 `xianma-ai-studio`、`xianma-centaur-desktop` 等历史目录。
- 卸载器只删除程序目录，不删除 `%APPDATA%` 和 ProgramData。

**后续同版本修复包**

- `memoryfix`：导入 `1.0.0` 起的全局、匿名、本地和钉钉用户会话键。
- `memoryfix2`：合并多个历史资料目录的会话索引，不删除旧 LevelDB。
- `startupfix`：修复安装后应用无法启动及用户数据路径选择问题。

## 1.0.3：企业用户权限与通用电脑操作版

```text
显示版本：1.0.3
内部版本：1.0.3
更新通道：stable-v2
状态：已发布
```

**主要改进**

- 新增通用 Windows 应用发现、启动、切换、截图、点击、输入和读取。
- 支持任意桌面应用任务，不限于飞书或微信。
- 新增软件使用统计和在线状态。
- 钉钉登录用户同步至 MAPMS，并按 MAPMS 状态控制登录权限。
- 继续修复从 `1.0.0` 起的会话记忆迁移。

**怎么实现**

- 使用 PowerShell + Windows UI Automation + Win32 消息操作标准控件。
- 自绘界面使用窗口截图、多模态识别和相对坐标操作。
- 发送消息采用一次性草稿，最终发送必须二次确认。
- 客户端登录后异步登记使用统计，每 60 秒发送心跳。
- 模型密钥、MAPMS 密钥和钉钉 Secret 保留在服务器或受控配置，不下发前端。
- MAPMS 先按钉钉 `userid` 查找绑定用户；缺失用户按规范注册，禁用或锁定用户拒绝登录。

**1.0.3 文档热修复候选（内部 1.0.3.1）**

- 修复 DOCX 同名文件被 Word 占用时下载失败。
- 短暂占用自动重试，持续占用自动另存为带序号文件。
- 清理文档中的 YAML、`#`、`**` 和代码围栏，转换为正式标题、列表和表格。
- 该热修复有本地安装包和说明，但现有记录不能证明它曾独立发布到服务器。

## 1.0.4：公司公共技能平台预研版

```text
显示版本：1.0.4
内部版本：1.0.4
更新通道：stable-v2
状态：服务端隐藏部署，客户端未发布
```

**主要改进**

- 新增公司技能创建向导和标准 ZIP 模板。
- 支持提交审核、我的提交、公司技能库和后台审核。
- 支持批准、驳回、高风险确认、下架和重新提交。
- 支持 SHA-256、Ed25519 签名、自动更新和下架隔离。
- 支持匿名统计技能调用、成功、失败和取消次数。
- 包含 DOCX 文件占用修复和文档排版优化。

**怎么实现**

- 客户端使用分片上传和后台任务提交技能包。
- 服务端进行 ZIP 安全检查、风险扫描、版本管理和签名发布。
- 公司技能使用独立 ID，避免与本地技能同名冲突。
- 客户端启动及每 15 分钟检查已安装公司技能更新。

**明确状态**

- 用户决定暂缓此大版本。
- 公司技能服务端已于 2026-08-14 隐藏部署，普通更新后台不展示审核入口。
- 后续 `1.3.0.2` 和 `1.3.0.3` 的前端入口、IPC 和打包文件均明确排除此模块。

## 1.3.0.2：东哥会议模型版

```text
显示版本：1.3.0.2
内部版本：1.3.0.2
更新通道：stable-v2
状态：已交付，作为 1.3.0.3 前序生产版本
```

**主要改进**

- 新增固定内置技能“东哥会议模型”。
- 技能位于自带技能第一位。
- 会议重点输出项只读展示，用户不能取消。
- 支持文本、录音、视频、PDF、PPT 和截图等会议素材。
- 默认生成 DOCX，也支持 PPTX、PDF、MD 和 TXT。
- 保留同名 DOCX 占用重试和文档排版优化。
- 明确不包含 `1.0.4 / 1.4.0` 公司技能审核发布功能。

**怎么实现**

- 保留历史技能 ID `meeting`，避免旧会话和任务失效。
- 完整 `SKILL.md` 与会议知识体系参考资料随 `app.asar` 打包。
- 主进程按技能 ID 强制注入完整规则，不依赖前端卡片描述。
- 打包配置排除公司技能客户端文件，Preload 不暴露公司技能 IPC。

## 1.3.0.3：模型路由、音视频和本机能力增强版

```text
真实内部版本：1.3.0.3
更新通道：stable-v2
状态：已上传更新服务器
```

**主要改进**

- 调整模型路由策略。
- 增加视频、录音和音频导入及处理能力。
- 增强本机应用、文件和命令操作能力。
- 保留“东哥会议模型”和从 `1.0.0` 起的数据迁移。

**怎么实现**

- 文字请求统一经过公司服务端路由。
- 主模型活跃请求达到 14 时，新请求切换备用企业模型服务；低于 14 后自动恢复。
- 满载时前端切换为 `Auto 自动选择`，不显示备用供应商和模型名称。
- 图片生成继续使用独立图片接口，不计入文字并发路由。
- 安装包内置 FFmpeg、Whisper 和中文语音模型，支持本地音视频抽取和转写。
- 所有会话继续拥有文件、技能、文档和应用操作能力。

**线上安装包**

```text
文件：交付\先马·AI Studio Setup 1.3.0.3.exe
大小：668619574 字节
SHA-256：ED150F3CDC608E64F2A0DCD48B9E025118A4C3E106CEFF4F6310FCB811C50F8A
数字签名：NotSigned
```

**线上元数据问题**

- 更新服务器清单错误地写成：显示版本 `1.0.3`、内部版本 `1.3.0.3`。
- 实际安装包、文件名和程序版本均为 `1.3.0.3`。
- 后续发布应修正显示版本，不能继续把 `1.0.3` 和 `1.3.0.3` 混用。

## 1.3.0.3 本地修正版：回答复制与图片话术

```text
状态：已构建、已测试、未上传服务器
```

**主要改进**

- 助手每条回答增加一键复制。
- 修复“上传图片并要求生成话术”被错误识别为生成图片的问题。
- 图片总结、分析、提取和文案任务改走多模态文字对话。
- 只有明确要求生成图片、海报、绘图或编辑图片时才走图片接口。
- 自动更新支持断点续传、失败重试、速度和剩余时间显示。
- 网络中断后保留 `.partial` 文件，不再从 0 开始下载。

**怎么实现**

- 回答复制通过 Electron 主进程写入 Windows 剪贴板。
- 图片路由从“只看是否有图片”改为“附件类型 + 用户动词 + 输出目标”联合判断。
- 图片理解请求把图片真实作为视觉数据发送给文字模型。
- 更新下载使用 Range 请求继续下载，并校验最终大小和 SHA-256。

**本地安装包**

```text
文件：dist-1.3.0.3\先马·AI Studio Setup 1.3.0.3 回答复制与图片话术修正版.exe
大小：789768729 字节
SHA-256：8FA6946F3FCA761C8D8C979592064C247F756FA2E09164419777CC3BE45595E5
数字签名：NotSigned
服务器状态：未上传
```

**重要限制**

- 此修正版仍使用内部版本 `1.3.0.3`。
- 已安装线上 `1.3.0.3` 的客户端不会把相同内部版本识别为新更新。
- 若要自动下发，必须改用高于 `1.3.0.3` 的新内部版本，例如 `1.3.0.4`。

## 七、当前发布状态

| 项目 | 当前状态 |
| --- | --- |
| 正式产品名 | 先马·AI Studio |
| 当前服务器最高内部版本 | `1.3.0.3` |
| 当前服务器更新通道 | `stable-v2` |
| 线上显示版本 | 错误写为 `1.0.3`，实际应为 `1.3.0.3` |
| 当前本地修正版 | `1.3.0.3` 回答复制与图片话术修正版 |
| 本地修正版是否上传 | 否 |
| 公司技能平台 | 预研代码存在，但未在 `1.3.0.2/1.3.0.3` 发布 |
| Windows 数字签名 | 尚未完成 |
| 更新协议 | 公网 IP + HTTP，仅适合内测 |
| macOS 客户端 | 尚未正式交付 |

## 八、主要能力演进总结

### 对话能力

```text
基础单会话
→ 多会话与历史记录
→ 流式输出和停止生成
→ 多会话并行
→ 模型切换与自动路由
→ 双服务并发路由
```

### 文件与本机能力

```text
附件上传
→ 文件卡和绝对路径
→ 文档生成、预览、复制和下载
→ 全磁盘文件操作
→ 命令运行和网页预览
→ 通用 Windows 应用操作
```

### 技能能力

```text
固定技能卡片
→ 本地 Markdown/ZIP/文件夹安装
→ 在线技能安装
→ 后台静默安装
→ 按会话选择技能
→ 技能真实流式结果和 DOCX
→ 固定内置东哥会议模型
```

### 登录与企业管理

```text
原型登录
→ 钉钉扫码登录
→ 钉钉用户本地数据隔离
→ 使用统计与在线心跳
→ MAPMS 用户同步和权限校验
```

### 更新能力

```text
手动发安装包
→ 客户端检查更新
→ 后台分片上传
→ SHA-256 + Ed25519 校验
→ 覆盖安装与数据保留
→ 断点续传和失败重试
```

## 九、已知问题与后续发版要求

1. 线上 `1.3.0.3` 显示版本元数据错误，需要在下一次发布时纠正。
2. 同一个内部版本不能重复用于自动更新；当前本地修正版必须升内部版本才能下发。
3. 历史同名安装包很多，分发前必须核对 SHA-256、内部版本和构建时间。
4. 不得修改 AppId、用户数据路由或历史目录兼容逻辑，否则旧会话可能再次不可见。
5. 安装和卸载不得删除 `%APPDATA%` 下的先马历史目录和 `C:\ProgramData\XianmaAIStudio`。
6. 公司公共技能平台仍属于未来大版本，不能写入 `1.3.0.2` 或现有 `1.3.0.3` 的已发布说明。
7. 正式全员发布前应完成 HTTPS 和 Windows Authenticode 数字签名。
8. 后续每次发布必须同时记录：显示版本、内部版本、通道、安装包大小、SHA-256、发布日期、更新说明和服务器状态。

## 十、建议的后续版本规则

```text
小修复：1.3.0.4、1.3.0.5……
公司技能平台正式发布：1.4.0
显示版本 = 内部版本
通道继续使用 stable-v2
同一通道、平台、架构下绝不复用内部版本
```

每次发布建议创建独立文档：

```text
文档\版本说明\先马·AI Studio <版本号> 发布说明.md
```

并保留：

```text
改进了什么
怎么实现
修复了什么
兼容和迁移说明
安装包文件名
安装包大小
SHA-256
数字签名状态
更新通道
是否已上传服务器
```
